version: "2"
fields:
  - title: Kovan Endpoint
    id: kovan-fraud-prover-endpoint
    description: The Ethereum RPC server that will be used by the verifier. These must be set to Ethereum nodes that have synced the Kovan testnet.
    target:
      type: environment
      name: L1_NODE_WEB3_URL
      service: fraud-prover
    required: true
  - title: Kovan Endpoint
    id: kovan-verifier-endpoint
    description: The Ethereum RPC server that will be used by the fraud prover. These must be set to Ethereum nodes that have synced the Kovan testnet.
    target:
      type: environment
      name: ETH1_HTTP
      service: verifier
    required: true